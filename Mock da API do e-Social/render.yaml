services:
  - type: web
    name: mock-esocial-api
    env: node
    region: us-east-1
    plan: free
    branch: main
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
